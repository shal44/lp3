`python -m venv .venv && .\.venv\Scripts\Activate.ps1 && pip install matplotlib pandas numpy scikit-learn`
`python3 -m venv .venv && source .venv/bin/activate && pip install matplotlib pandas numpy scikit-learn`
